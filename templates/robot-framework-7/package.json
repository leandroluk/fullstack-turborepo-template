{
  "name": "@templates/robot-framework-7",
  "version": "0.0.1",
  "description": "Template de projeto de testes end-to-end usando Robot Framework e já configurado com configs de monorepo",
  "private": true,
  "scripts": {
    "cluster:up": "k3d cluster create e2e-stack --wait",
    "cluster:down": "k3d cluster delete e2e-stack",
    "test:e2e": "uv run robot --outputdir ../../reports ./tests",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "format": "uv run ruff format .",
    "typecheck": "uv run pyright ."
  }
}