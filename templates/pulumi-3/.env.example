# Stack to deploy — local | baremetal | aws | gcp
STACK=local

# Container registry and image tag
REGISTRY=localhost:5000
TAG=latest

# k3d cluster (local only)
CLUSTER_NAME=fullstack
REGISTRY_NAME=k3d-registry.localhost
REGISTRY_PORT=5000
